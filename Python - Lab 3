
#lab 3

import socket
import time

HOST = input('Enter ip address: ') #hostname in internet domain notation; IPv4 is 127.0.0.1
PORT = 5000                          #port is an integer; I used 500 bc the lab wants us to scan 500 ports

sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
result = sock.connect_ex((HOST, PORT))

start_time = time.time()
if __name__ == "__main__":

   print('Time taken: ', time.time()- start_time)


if result == 0:
   print ("Open")
else:
  exit(0)


#socket.close()
