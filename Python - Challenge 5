#challenge 05

g = 9.8

def main():
    print("We will determine the distance an object has traveled while in a state of free fall.")
    time_of_fall = int(input("Enter an amount of time the object will be falling in seconds: "))

    distance_traveled_per_second = fallingDistance(time_of_fall)
    

def fallingDistance(t):

    for d in range (1,t+1): 
        
        distance = 0.5 * g * (d**2)
        print(distance)
    

main() 
